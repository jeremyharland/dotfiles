# Shell config (aliases, completion, autosuggestions, syntax-highlighting,
# fzf bindings, prompt/theme) is managed declaratively by nix-darwin — see
# ~/dotfiles/.config/nix/flake.nix (programs.zsh). Run `p10k configure`
# once to generate ~/.p10k.zsh.
